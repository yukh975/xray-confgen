🌐 [English](README.md) | **Русский**

---

<img src="assets/logo.svg" width="48" height="48" align="left" style="margin-right: 14px">

# Xray config generator v1.0.4

[![PHP](https://img.shields.io/badge/PHP-8.1%2B-777bb4)](https://www.php.net)
[![Nginx](https://img.shields.io/badge/web-Nginx%20%2B%20PHP--FPM-009639)](https://nginx.org)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE.md)

**Веб-инструмент для генерации `config.json` для [xray-core](https://github.com/XTLS/Xray-core) из одного или нескольких VLESS URI.**

Добавьте один или несколько VLESS URI, задайте параметры SOCKS5 inbound, настройте правила маршрутизации и при необходимости балансировщик нагрузки — и получите готовый `config.json`, который можно скачать или скопировать.

---

## Демо

Текущая версия для разработки доступна по адресу **https://yukh.net/xray-confgen/**

---

## Использование

Для базовой настройки достаточно вставить **VLESS URI** в соответствующее поле и нажать **«Сгенерировать config.json»**. Файл сразу готов к использованию с xray-core.

Тем не менее мы рекомендуем уделить несколько минут более тонкой настройке клиента. Доступные параметры описаны ниже.

**Как маршрутизация и DNS зависят друг от друга:**
- Без **маршрутизации** — весь трафик уходит через прокси без какой-либо фильтрации. Клиент не различает локальный, иностранный или нежелательный трафик.
- Без **DNS** — xray-core использует системный DNS-резолвер. DNS-запросы выполняются вне туннеля и могут быть видны провайдеру (утечка DNS). Правила маршрутизации, зависящие от резолвинга доменов (стратегии `IPIfNonMatch` / `IPOnDemand`), будут использовать системный резолвер, что может давать некорректные результаты.
- **Маршрутизация без DNS** — работает, но доменная маршрутизация зависит от системного резолвера. Есть риск утечки DNS-запросов.
- **DNS без маршрутизации** — xray-core использует настроенные резолверы, но весь трафик всё равно идёт через прокси. Защита от утечек DNS работает, разделение трафика — нет.
- **Оба включены** — полный контроль: трафик разделяется по правилам, DNS-запросы обрабатываются настроенными резолверами. Это рекомендуемая конфигурация.

### Inbound

**Inbound** определяет локальный прокси, который xray-core открывает на вашем устройстве. Приложения (браузер, система) подключаются к нему, а xray-core пересылает их трафик через VLESS-туннель.

**IP-адрес** определяет, на каком сетевом интерфейсе xray-core будет принимать подключения:
- `127.0.0.1` — рекомендуется для личных устройств (компьютеры, ноутбуки, телефоны). Подключиться к прокси может только само устройство; из локальной сети он недоступен.
- `0.0.0.0` — прослушивает все доступные IP-адреса на устройстве. Другие устройства в той же сети смогут использовать этот прокси. Используйте осторожно.

**Порт** — локальный порт, на котором слушает SOCKS5-прокси. Значение по умолчанию `10808` подходит в большинстве случаев; измените его при конфликте с другим приложением.

**HTTP inbound** — нажмите *Добавить HTTP inbound*, чтобы добавить второй локальный прокси, принимающий запросы HTTP CONNECT (в дополнение к SOCKS5). Это полезно для приложений, которые поддерживают HTTP-прокси, но не поддерживают SOCKS5. Адрес по умолчанию: `127.0.0.1:8080`. IP и порт настраиваются так же, как для SOCKS5. Нажмите ✕, чтобы удалить HTTP inbound.

### VLESS

В этом разделе можно добавить один или несколько VLESS URI — по одному на каждый удалённый сервер. Каждая запись содержит необязательное поле **Имя** и сам URI. Имя становится тегом outbound, который используется в правилах маршрутизации; если поле оставить пустым, теги назначаются автоматически: `proxy` для первой записи, `proxy2`, `proxy3`, … для остальных. Нажмите **+ Добавить VLESS**, чтобы добавить ещё один сервер. Нажмите ✕, чтобы удалить запись.

Поддерживаются следующие форматы URI:

- **TCP** — TCP-транспорт с TLS или Reality:
  `vless://uuid@host:port?security=reality&flow=xtls-rprx-vision&pbk=...&sid=...&fp=chrome#name`
- **WebSocket (WS)** — WebSocket-транспорт, как правило с TLS:
  `vless://uuid@host:port?security=tls&type=ws&path=/ws&host=example.com#name`
- **XHTTP / SplitHTTP** — мультиплексированный HTTP-транспорт:
  `vless://uuid@host:port?security=tls&type=xhttp&path=/&mode=auto#name`
- **gRPC** — gRPC-транспорт с опциональным multi-режимом:
  `vless://uuid@host:port?security=tls&type=grpc&serviceName=myservice#name`
- **HTTP/2 (h2)** — HTTP/2-транспорт с TLS:
  `vless://uuid@host:port?security=tls&type=h2&path=/&host=example.com#name`

Поддерживаемые типы безопасности: `none`, `tls`, `reality`. Фрагмент после `#` сохраняется в конфиге как читаемый комментарий.

### Balancer

При наличии нескольких VLESS-записей раздел **Balancer** позволяет автоматически распределять трафик между серверами. При включении балансировщика появляется виртуальный outbound `balancer`, который можно выбрать в правилах маршрутизации или в качестве маршрута по умолчанию.

Доступны две стратегии:
- **random** — каждое соединение направляется на случайно выбранный сервер. Измерение задержки не требуется.
- **leastPing** — каждое соединение направляется на сервер с наименьшей измеренной задержкой. Требует **observatory**, которое периодически проверяет доступность серверов. Настройте URL проверки (по умолчанию: `https://www.google.com/generate_204`) и интервал (по умолчанию: `10s`).

При отключённом балансировщике правила маршрутизации ссылаются непосредственно на теги отдельных серверов.

### Sniffing

Сниффинг позволяет xray-core анализировать первые байты входящего трафика и определять протокол прикладного уровня. Определённый протокол используется для более точной маршрутизации, а при необходимости — для подмены адреса назначения, извлечённого из рукопожатия. Включён по умолчанию.

**Определять протоколы (destOverride)** — протоколы, которые xray-core будет пытаться идентифицировать:
- `http` — обычный HTTP/1.x-трафик.
- `tls` — TLS-рукопожатие; xray-core извлекает SNI из ClientHello для маршрутизации. Рекомендуется оставить включённым.
- `quic` — трафик QUIC/HTTP3.
- `bittorrent` — протокол BitTorrent; необходим, если вы хотите маршрутизировать или блокировать торренты по протоколу.

**Только для маршрутизации (routeOnly)** — при включении определённый протокол используется только для принятия решений о маршрутизации. xray-core не будет подменять оригинальный адрес назначения на тот, что извлечён из рукопожатия.

### Databases

В этом разделе отображается список геобаз данных, доступных на сервере. Они используются при настройке правил маршрутизации и DNS для сопоставления трафика по стране, региону или категории.

### Routing

Маршрутизация определяет, как xray-core обрабатывает каждое соединение — отправить через прокси, пустить напрямую или заблокировать. Если раздел отключён, весь трафик идёт через прокси без фильтрации.

**Пресеты** — готовые наборы правил, которые можно комбинировать. Нажмите *Пресеты* и отметьте нужные галочками:
- **Россия** — локальный и российский трафик напрямую, реклама заблокирована. Устанавливает маршрут по умолчанию `proxy`.
- **Иран** — локальный и иранский трафик напрямую (требует `geoip_IR.dat` / `geosite_IR.dat`).
- **Блокировать рекламу** — блокирует домены из `category-ads-all`.
- **Всё через прокси** — включает маршрутизацию с маршрутом по умолчанию `proxy`, без добавления правил.
- **Блокировать BitTorrent** — добавляет правило блокировки всего BitTorrent-трафика на уровне протокола.

Пресеты добавляют правила, не удаляя существующие. Дубликаты пропускаются. При отключении пресета удаляются только добавленные им правила. Кнопка *Очистить правила* сбрасывает всё сразу.

**Маршрут по умолчанию** — действие для трафика, который не попал ни под одно правило. В выпадающем списке отображаются теги VLESS-серверов (например, `proxy`, `proxy2`, …), `direct`, и — при включённом Balancer — `balancer`:
- **Тег VLESS-сервера** — пустить несовпавший трафик через этот конкретный сервер.
- `direct` — пустить несовпавший трафик напрямую без туннеля.
- `balancer` — распределять несовпавший трафик по всем серверам (доступно только при включённом Balancer).

**Стратегия доменов** определяет, как xray-core обрабатывает доменные имена перед применением правил маршрутизации:
- `IPIfNonMatch` — сначала проверяются правила по домену; если ни одно не совпало, домен резолвится в IP и проверяются IP-правила. Рекомендуется для большинства случаев.
- `IPOnDemand` — домен всегда резолвится в IP перед маршрутизацией. Более точно, но требует дополнительного DNS-запроса для каждого соединения.
- `AsIs` — маршрутизация только по доменному имени, без резолвинга. Быстрее всего, но IP-правила не применяются к доменному трафику.

**Правила маршрутизации** проверяются сверху вниз — срабатывает первое совпавшее правило. Каждое правило состоит из:
- **База данных** — геобаза для сопоставления (`geosite.dat` — домены и категории, `geoip.dat` — IP-адреса и подсети). Доступны также кастомные базы из раздела Databases.
- **Теги** — одна или несколько категорий из выбранной базы (например, `ru`, `private`, `category-ads-all`). Нажмите на поле, чтобы открыть список, воспользуйтесь поиском или введите своё значение.
- **Действие** — что делать с совпавшим трафиком: тег VLESS-сервера (например, `proxy`, `proxy2`), `direct`, `block` или `balancer` (при включённом Balancer).

Типичная настройка: применить пресет *Россия* и установить маршрут по умолчанию в тег первого VLESS-сервера (`proxy`).

### DNS

Раздел позволяет настроить собственный DNS-резолвер для xray-core. Если раздел отключён, используется системный DNS. Настройка DNS рекомендуется для предотвращения утечек и для направления разных доменов в разные резолверы.

**Резервный DNS** — резолвер, который используется когда ни одно DNS-правило не совпало с запрашиваемым доменом. Выберите один из пресетов (Google, Cloudflare, Yandex) или введите свой адрес.

**Стратегия запросов** — определяет, какие типы IP-адресов xray-core запрашивает у DNS:
- `UseIPv4` — запрашивать только A-записи (IPv4). Рекомендуется для большинства случаев.
- `UseIPv6` — запрашивать только AAAA-записи (IPv6).
- `UseIP` — запрашивать и A, и AAAA-записи.
- `ForceIPv4` / `ForceIPv6` / `ForceIP` — то же, что выше, но результат принудительно приводится к нужному типу, даже если сервер вернул другой.
- `useSystem` — передать все DNS-запросы операционной системе.

**Стратегия доменов** — определяет, как xray-core резолвит домены при применении DNS-правил, по той же логике, что и стратегия доменов в маршрутизации (`IPIfNonMatch`, `IPOnDemand`, `AsIs`).

**DNS-серверы** — список резолверов, которые будет использовать xray-core. Каждый сервер может быть:
- **Пресетом** — Google DoH, Cloudflare DoH, Yandex DoH (все используют IP-адреса, чтобы не зависеть от начального резолвера) или их обычные DNS-аналоги.
- **Кастомным** — укажите имя (используется как метка в правилах) и адрес: обычный IP (`8.8.8.8`) или DoH URL (`https://1.1.1.1/dns-query`).

Каждый пресет можно добавить только один раз. При нажатии *Добавить сервер* автоматически выбирается первый незанятый пресет. При попытке вручную выбрать пресет, который уже есть в списке, выводится ошибка и выбор возвращается к предыдущему значению.

**Правила DNS** работают так же, как правила маршрутизации, но вместо действия указывают на один из настроенных DNS-серверов. Каждое правило состоит из базы данных, одного или нескольких тегов и целевого сервера. Например, можно отправлять все домены зоны `ru` на Yandex DNS, а всё остальное резолвить через Cloudflare DoH.

### Mux

Mux (мультиплексирование) упаковывает несколько логических потоков в одно физическое TCP-соединение с сервером. Вместо того чтобы открывать новое соединение на каждый запрос — с отдельным TLS-рукопожатием — xray-core переиспользует одно соединение, снижая задержку для коротких и частых запросов (вкладки браузера, API-вызовы).

Mux наиболее эффективен при высоком пинге до сервера. При передаче больших файлов или стриминге может снизить производительность из-за head-of-line blocking. По умолчанию отключён.

> **⚠ Несовместим с Reality + XTLS flow.** Если VLESS URI использует безопасность Reality с параметром `flow=xtls-rprx-vision`, настройка Mux игнорируется и блок `mux` в конфиг не записывается.

**Concurrency** — максимальное число TCP-потоков в одном соединении. По умолчанию: 8.

**XUDP concurrency** — то же ограничение для UDP-потоков (протокол XUDP). По умолчанию: 8.

**UDP/443 (QUIC)** — обработка QUIC-трафика:
- `reject` — блокировать QUIC; клиент переключится на TCP/TLS. Рекомендуется в большинстве случаев.
- `allow` — проксировать QUIC через туннель.
- `skip` — пустить QUIC напрямую, без туннеля.

### Импортировать config.json

Кнопка **Импортировать config.json** позволяет загрузить существующий файл `config.json` обратно в форму. Нажмите кнопку и выберите файл — все поля (inbound, VLESS URI, правила маршрутизации, DNS и журналирование) будут заполнены автоматически. После этого можно скорректировать настройки и сгенерировать обновлённый `config.json`.

Корректный импорт гарантирован только для файлов, созданных этим инструментом. Конфиги, отредактированные вручную, могут импортироваться частично, если содержат структуры, которые генератор не производит.

### Поделиться

Кнопка **Поделиться** кодирует всё состояние формы — VLESS URI, настройки inbound, правила маршрутизации, конфигурацию DNS и параметры журналирования — в URL-безопасный параметр (`?s=…`) и копирует ссылку в буфер обмена. При открытии ссылки на любом устройстве конфигурация восстанавливается автоматически. Параметр удаляется из адресной строки после восстановления.

### Logging

При включении xray-core записывает события доступа и ошибки в лог-файл. Это удобно для диагностики проблем с подключением.

**Каталог журналов** — путь к папке, в которой будет создан файл журнала (`xray-core.log`). Каталог необходимо создать вручную на клиентском устройстве до запуска xray-core — он не будет создан автоматически. Оставьте поле пустым, чтобы выводить логи в stdout/stderr вместо файла.

**Уровень журналирования** — управляет детализацией лога:
- `debug` — максимальная детализация, включая внутренние события xray-core. Используйте только при отладке.
- `info` — общая информация о соединениях и активности сервера.
- `warning` — только потенциально проблемные события. Рекомендуется для обычного использования.
- `error` — только критические ошибки, препятствующие корректной работе xray-core.
- `none` — журналирование полностью отключено.

---

## Требования

- Nginx + PHP-FPM (PHP 8.1+)
- `.dat` файлы геобаз в директории `db/`

---

## Установка

```bash
git clone https://github.com/yukh975/xray-confgen.git
cd xray-confgen
```

Директория `db/` с файлами геобаз включена в репозиторий:

```
xray-confgen/
└── db/
    ├── geosite.dat
    ├── geoip.dat
    ├── geosite_RU.dat
    └── geoip_RU.dat
```

Все файлы `.dat`, находящиеся в директории `db/`, определяются и используются автоматически.

Для обновления или добавления баз данных скачайте нужные файлы в директорию `db/`:

| Файл | Источник |
|------|----------|
| `geoip.dat` | [Loyalsoldier/v2ray-rules-dat](https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat) |
| `geosite.dat` | [Loyalsoldier/v2ray-rules-dat](https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat) |
| `geoip_IR.dat` | [chocolate4u/Iran-v2ray-rules](https://github.com/chocolate4u/Iran-v2ray-rules/releases/latest/download/geoip.dat) |
| `geosite_IR.dat` | [chocolate4u/Iran-v2ray-rules](https://github.com/chocolate4u/Iran-v2ray-rules/releases/latest/download/geosite.dat) |
| `geoip_RU.dat` | [runetfreedom/russia-v2ray-rules-dat](https://raw.githubusercontent.com/runetfreedom/russia-v2ray-rules-dat/release/geoip.dat) |
| `geosite_RU.dat` | [runetfreedom/russia-v2ray-rules-dat](https://raw.githubusercontent.com/runetfreedom/russia-v2ray-rules-dat/release/geosite.dat) |

Настрой nginx:

```nginx
# Ограничение частоты запросов — поместить в блок http {}
limit_req_zone $binary_remote_addr zone=xray_api:10m rate=20r/s;

server {
    listen 443 ssl;
    server_name your.domain.com;

    ssl_certificate     /path/to/ssl/certificate.crt;
    ssl_certificate_key /path/to/ssl/certificate.key;

    root  /path/to/xray-confgen;
    index index.html;

    # Заголовки безопасности
    add_header X-Content-Type-Options  "nosniff"           always;
    add_header X-Frame-Options         "DENY"              always;
    add_header Referrer-Policy         "no-referrer"       always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; font-src 'self';" always;

    location /api/ {
        limit_req zone=xray_api burst=30 nodelay;

        fastcgi_pass unix:/run/php/php-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

Если путь к `db/` отличается — измени константу `GEO_DIR` в файлах `api/tags.php` и `api/databases.php`.

---

## Структура проекта

```
xray-confgen/
├── index.html              # Веб-интерфейс
├── assets/
│   ├── app.js              # Логика фронтенда
│   ├── translations.js     # Переводы интерфейса (EN / RU)
│   └── style.css           # Стили
├── api/
│   ├── parse.php           # Парсинг VLESS URI + генерация config.json
│   ├── tags.php            # Чтение категорий из .dat файлов
│   └── databases.php       # Список доступных баз геоданных
└── db/
    └── *.dat               # Файлы геобаз
```

---

## API

### `POST /api/parse.php`

Генерирует `config.json`.

**Тело запроса:**
```json
{
  "inbound_ip": "0.0.0.0",
  "inbound_port": 10808,
  "vless_link": "vless://uuid@host:port?...",
  "routing_rules": [
    { "rule_type": "ip",     "db": "geoip.dat",   "values": ["private"], "action": "direct" },
    { "rule_type": "domain", "db": "geosite.dat",  "values": ["ru"],      "action": "direct" }
  ]
}
```

### `GET /api/tags.php?db=geosite.dat`

Возвращает список категорий из указанного `.dat` файла.

### `GET /api/databases.php`

Возвращает список доступных `.dat` файлов из директории `db/`.

---

## Автор

Юрий Хачатурян (при поддержке [Claude.AI](https://claude.ai)), 2026.

---

🌐 [English](README.md) | **Русский**
